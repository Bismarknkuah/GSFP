{
  "name": "gsfp-backend-v2",
  "version": "2.0.0",
  "description": "Ghana School Feeding Programme — National Management System v2 Backend",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/db/seed.js --force",
    "seed:fresh": "node -e \"require('dotenv').config(); const mongoose=require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async()=>{ await mongoose.connection.dropDatabase(); console.log('DB dropped'); process.exit(0); }).catch(e=>{console.error(e);process.exit(1);})\" && node src/db/seed.js --force",
    "build": "echo 'No build step required for Node.js backend'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "csv-parse": "^6.2.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.3",
    "totp-generator": "^1.0.1",
    "twilio": "^6.0.2",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
